# predict.py
from flair.models import SequenceTagger
from flair.data import Sentence
from razdel import tokenize
import sys

# === КОНСТАНТЫ ===
MODEL_PATH = "ner_model/final-model.pt"
TEST_FILE = "test.txt"  # замените на нужный путь

def text_to_sentence(text: str) -> Sentence:
    """Преобразует текст в Sentence с токенизацией через razdel."""
    tokens = [t.text for t in tokenize(text)]
    sentence = Sentence()
    for token in tokens:
        sentence.add_token(token)
    return sentence

def main():
    # Загружаем модель
    tagger = SequenceTagger.load(MODEL_PATH)

    # Читаем тестовый файл
    with open(TEST_FILE, "r", encoding="utf-8") as f:
        text = f.read()

    # Создаём предложение
    sentence = text_to_sentence(text)

    # Предсказание
    tagger.predict(sentence)

    # Вывод результатов
    print(f"\n🔍 Результаты для файла: {TEST_FILE}\n")
    for entity in sentence.get_spans("ner"):
        print(f"[{entity.tag}] {entity.text} (уверенность: {entity.score:.3f})")

if __name__ == "__main__":
    main()
